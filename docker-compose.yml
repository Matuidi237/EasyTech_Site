services:
  thinktech-site:
    build: .
    container_name: thinktech-site
    restart: unless-stopped
    networks:
      - webproxy
    env_file:
      - .env

networks:
  webproxy:
    external: true
